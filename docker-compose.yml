version: '3'

services:

  client:
    build: ./client

    ports:
      - "5173:5173"

  server:
    build: ./server

    ports:
      - "5000:5000"

    environment:
      - MONGO_URI=your_mongodb_uri

  mongo:
    image: mongo

    ports:
      - "27017:27017"